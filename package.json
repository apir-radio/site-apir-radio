{
  "name": "site-apir-radio",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "npm run content:generate && next dev",
    "jobs:generate": "node scripts/generate-jobs.mjs",
    "events:generate": "node scripts/generate-events.mjs",
    "content:generate": "npm run jobs:generate && npm run events:generate",
    "content:check": "node scripts/generate-jobs.mjs --check && node scripts/generate-events.mjs --check",
    "links:check": "node scripts/check-links.mjs out",
    "build": "npm run build:pages",
    "build:pages": "npm run content:generate && GITHUB_PAGES=true next build --webpack",
    "test": "npm run build:pages && node --test tests/rendered-html.test.mjs",
    "lint": "eslint . --ignore-pattern .next"
  },
  "dependencies": {
    "next": "16.2.6",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "4.2.1",
    "@types/node": "26.2.0",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.4",
    "eslint": "9.39.4",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "4.3.3",
    "typescript": "5.9.3"
  },
  "type": "module"
}
